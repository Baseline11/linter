/**
 * Contains the configuration for the Strict mode, as defined in the ESLint documentation.
 *
 * @link http://eslint.org/docs/rules/#strict-mode
 * @type {Object}
 */
module.exports = {
  rules: {
    strict: ['error', 'never']
  }
};
